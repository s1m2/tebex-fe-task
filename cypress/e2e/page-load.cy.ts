it('Page Loads Successfully', function() {
    cy.visit('http://localhost:8080/')
    
    cy.get('[data-testid="section-primary"] img.mb-8').should('be.visible');
    cy.get('[data-testid="section-primary"] img.mb-8').should('have.attr', 'alt', 'Tebex Logo');
    cy.get('[data-testid="order-container-title"]').should('be.visible');
    cy.get('[data-testid="order-container-title"]').should('have.text', 'Your order');
    cy.get('[data-testid="section-primary"] div.mx-auto').should('be.visible');
    cy.get('[data-testid="order-item-name-Medium Booster"]').should('be.visible');
    cy.get('[data-testid="order-item-name-Medium Booster"]').should('have.text', 'Medium Booster');
    cy.get('[data-testid="order-item-quantity-Medium Booster"]').should('be.visible');
    cy.get('[data-testid="order-item-quantity-Medium Booster"]').should('have.text', 'Qty: 1');
    cy.get('[data-testid="order-item-price-Medium Booster"]').should('be.visible');
    cy.get('[data-testid="order-item-price-Medium Booster"]').should('have.text', '9.99 USD ');
    cy.get('[data-testid="order-item-name-Small Coins"]').should('be.visible');
    cy.get('[data-testid="order-item-name-Small Coins"]').should('have.text', 'Small Coins');
    cy.get('[data-testid="order-item-image-Small Coins"]').should('be.visible');
    cy.get('[data-testid="order-item-price-Small Coins"]').should('be.visible');
    cy.get('[data-testid="order-item-price-Small Coins"]').should('have.text', '4.99 USD ');
    cy.get('[data-testid="label-code"]').should('be.visible');
    cy.get('[data-testid="label-code"]').should('have.text', 'Coupon/Gift Card');
    cy.get('[data-testid="input-code"]').should('be.visible');
    cy.get('[data-testid="apply-coupon-button"]').should('be.visible');
    cy.get('[data-testid="apply-coupon-button"]').should('have.text', 'Confirm');
    cy.get('[data-testid="amount-row-label-Subtotal:"]').should('be.visible');
    cy.get('[data-testid="amount-row-label-Subtotal:"]').should('have.text', 'Subtotal:');
    cy.get('[data-testid="amount-row-amount-Subtotal:"]').should('be.visible');
    cy.get('[data-testid="amount-row-amount-Subtotal:"]').should('have.text', '14.98 USD');
    cy.get('[data-testid="amount-row-label-Sales Tax:"]').should('be.visible');
    cy.get('[data-testid="amount-row-label-Sales Tax:"]').should('have.text', 'Sales Tax:');
    cy.get('[data-testid="amount-row-amount-Sales Tax:"]').should('be.visible');
    cy.get('[data-testid="amount-row-amount-Sales Tax:"]').should('have.text', '3 USD');
    cy.get('[data-testid="amount-row-label-Total:"]').should('be.visible');
    cy.get('[data-testid="amount-row-label-Total:"]').should('have.text', 'Total:');
    cy.get('[data-testid="amount-row-amount-Total:"]').should('be.visible');
    cy.get('[data-testid="amount-row-amount-Total:"]').should('have.text', '17.98 USD');
    cy.get('[data-testid="breadcrumb-Make Payment"]').should('be.visible');
    cy.get('[data-testid="breadcrumb-Make Payment"]').should('have.text', 'Make Payment');
    cy.get('[data-testid="breadcrumb-Order Confirmed"]').should('be.visible');
    cy.get('[data-testid="breadcrumb-Order Confirmed"]').should('have.text', 'Order Confirmed');
    cy.get('[data-testid="label-email"]').should('be.visible');
    cy.get('[data-testid="label-email"]').should('have.text', 'Email');
    cy.get('[data-testid="input-email"]').should('be.visible');
    cy.get('[data-testid="label-cardNumber"]').should('be.visible');
    cy.get('[data-testid="label-cardNumber"]').should('have.text', 'Card number');
    cy.get('[data-testid="label-expiry"]').should('be.visible');
    cy.get('[data-testid="label-expiry"]').should('have.text', 'Expiry date');
    cy.get('[data-testid="input-expiry"]').should('be.visible');
    cy.get('[data-testid="label-cvv"]').should('be.visible');
    cy.get('[data-testid="label-cvv"]').should('have.text', 'CVV');
    cy.get('[data-testid="input-cvv"]').should('be.visible');
    cy.get('[data-testid="label-zip"]').should('be.visible');
    cy.get('[data-testid="label-zip"]').should('have.text', 'ZIP code / Postal code');
    cy.get('[data-testid="label-nameOnCard"]').should('be.visible');
    cy.get('[data-testid="label-nameOnCard"]').should('have.text', 'Name on card');
    cy.get('[data-testid="input-nameOnCard"]').should('be.visible');
    cy.get('[data-testid="pay-by-card-button"]').should('be.visible');
    cy.get('[data-testid="pay-by-card-button"]').should('have.text', 'Pay by Card');
});